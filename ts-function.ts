//function
const soma = (x: number, y: number): number => {
  return x + y;
};

const nomrmalizeText = (text: string) => {
  return text.trim().toLowerCase();
};

const showName: (name: string) => void = (name: string) => {
  console.log(name);
};
